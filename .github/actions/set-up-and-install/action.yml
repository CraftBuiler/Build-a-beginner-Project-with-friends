name: Install and test
description: Set up Node.js environment, install dependencies, and run tests.

runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v4
    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: "22"
        cache: "npm"
    - shell: bash
      run: npm ci